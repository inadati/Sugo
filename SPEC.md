# Sugo 仕様書

Sugo（スーゴ）は、AIハーネスを「双六（すごろく）盤を組み立てるように」作るための **Tauri製デスクトップアプリ** である。
ハーネスは「マス（cell）」を節点とする有向グラフであり、各マスには登録されたプロンプトが紐づく。マス間には分岐・ループ・ガード・始端/終端があり、全体として有限状態機械（FSM）として進行する。

Sugo は **Nipper**（Claude Code を PTY 起動するチャットUIデスクトップアプリ）と連携し、Nipper 内で動く Claude Code エージェントにハーネスを実行・著作させる。

---

## 1. 全体像

- **Sugo の役割**: ハーネス（盤面）の作成・編集・永続化・実行進行の制御
- **Nipper の役割**: Claude Code を PTY で起動し、チャットUIとして提供。ハーネスの「ターン」を駆動
- **Claude Code の役割**: マスのプロンプトに従って実作業を行う「頭脳」。マスの著作・整合修正も担う

Nipper の `chot-harness`（ハードコードされた参照FSMハーネス）を設計上の参照実装とする。Sugo は chot と同様に **システム側で確実に進行を制御する**構造を採る。

---

## 2. 決定台帳

### 決定1: 形態
- Tauri デスクトップアプリ（Nipper と同様）
- ハーネス＝マスの有向グラフ。分岐・ループ・ガード・始端/終端を持つ

### 決定2: 完了検知
- Sugo は PTY を持たないため、ターン完了を直接観測できない
- `sugo_advance` MCP 呼び出し ＋ プロジェクトの jsonl 監視（ストール検知）で進行を駆動する
- Claude Code のターン終了は `~/.claude/projects/<sanitized>/<session-id>.jsonl` への書き込み（`result/success`）で判定できる
- chot 同様、システム駆動で確実に回す

### 決定3: Nipper 連携
- Nipper が公開するローカル HTTP の inject API 経由で指示を渡す
- バッファし、エージェントがアイドルになったタイミングで注入する
- ローカル限定のためトークン認証は不要

### 決定4: ルーティング鍵
- session-id は再開（resume）でフォークするため不安定
- **プロジェクトパス**を安定鍵としてルーティングする

### 決定5: パス曖昧性（`-` 区切り）の解決
- Claude Code のプロジェクトディレクトリ名は `path.replace('/', '-')` で生成され、ロッシー（曖昧）
- jsonl 各レコードの `cwd` フィールド（実絶対パス）で照合する
- これは Claude Code 自身が採用している曖昧性解消の仕組みである

### 決定6: 排他の権威 / 永続化
- Sugo は SQLite DB を持つ
- `.sugo/` 共有ファイルは廃止し、調整に必要な情報はすべて DB に集約する
- DB が排他制御の権威となる（同一ハーネスの同時編集は楽観的ロックで制御）

### 決定7: 進行制御
- 完全システム駆動で進行を制御する（chot と同じ思想）
- エージェント操作に委ねる部分のみ chot に合わせる

### 決定8: 判定方式（chot 準拠）
- 評価フェーズ: システム側で集計判定（エージェントの自己申告を鵜呑みにしない）
- ルーティング: エージェントの自己申告（`signal.decision`）を採用
- chot-harness と同一の方式

### 決定9: DB 設計（ハイブリッド）
- **盤面定義（board definition）**: 不変な JSON ドキュメントとして `board_versions` に保存
  - 理由: スキーマが最も固まっていない領域であり、全テーブル化するとマイグレーションが頻発する。異種・入れ子の判定仕様は関係モデルに馴染まない。SQLite の弱い ALTER は破壊的なテーブル再構築を招く
- **実行状態 / レジストリ / イベント / outbox**: 正規化テーブル（クエリ・追記が必要なもの）
- 実行（run）は特定の board_version にピン留めされ、盤面の内部ID整合性を保つ

### 決定10: MCP ツール群
- ハーネスのマップ全体＋現在地を取得するツール（進行に必要な情報をエージェントへ渡す）
- `sugo_edit_cell`（マスのプロンプト編集）
- `sugo_validate_harness`（整合性検証: エッジ・到達性・終端の構造破綻を検出）
- `sugo_update_harness`（整合性修正の反映）
- `sugo_start`（ハーネス実行開始）
- `sugo_status`（現状取得・差分提示）
- `sugo_advance`（進行）
- 実装過程で必要な追加ツール・受け渡し情報が判明し得ることを前提に、DB 設計は将来の変更を見据える

### 決定10b: 手動マス追加とドラフト
- フロントエンドでユーザーが手動でマスを追加できる
- 追加されたマスは `status: draft` となり、エージェントが著作・整合修正するまで正規マスとして登録されない（runnable ハーネスから除外）
- `sugo_status` の差分により、追加されたマスが何かをエージェントと素早く認識共有する
- ドラフトの著作・整合修正が完了したら `draft → active` に昇格

### 決定10c: ドラフト一括解決＋実行ガード
- ドラフトを持つハーネスが発見されたら問題を先送りしない
- エージェントは **全ハーネス**を走査し、整合性修正とマス内容の充填を行う
- 完了後、修正したハーネスと修正内容をユーザーに報告する
- ドラフトを含むハーネスが実行された場合、`sugo_start` はハードエラーを返し、ドラフト確定と整合修正を促す

### 決定11: プロンプト登録時の制約とスキル配布
- マスへのプロンプト登録時、エージェントは2つの制約を守る:
  1. **ハーネス全体の整合性を踏まえる**（`sugo_validate_harness` が構造破綻を検出。プロンプトの意味的整合はエージェント判断）
  2. **ユーザーに意思決定を求める**（後戻りしにくい設計判断は勝手に確定せず確認する）
- これらは「エージェントの行動規律」であり、**スキルとして明文化**する
- 配布方式: Sugo は単一リポジトリとし、`Sugo/claude-plugins/<plugin>/skills/<skill>/SKILL.md` にプラグイン＋スキルを同梱する
  - Nipper の `claude-plugins/chot-harness/` と完全に同じ構成
  - MCP ツール（道具）とスキル（使い方の規律）が同一リポジトリで一緒にバージョン管理される

---

## 3. 参照実装（Nipper / chot-harness）

- Nipper は単一リポジトリ。スキルは `Nipper/claude-plugins/<plugin>/skills/<skill>/SKILL.md` に配置
  - `claude-plugins/chot-harness/`（ハーネス用スキル群）
  - `claude-plugins/confirm-one/`
- chot のターン終了検知: `result/success` ストリームイベント → `run_chot_step`
- chot の判定: 評価はシステム側集計（`chot_eval::aggregate`）、ルーティングは `signal.decision`
- chot の設定: SQLite から取得（例: `chot_max_cycles`、デフォルト50）
- 遷移種別: Wait / Halt / Done / Launch（Launch は Vue へ `chot-launch-phase` を emit し PTY 注入）
